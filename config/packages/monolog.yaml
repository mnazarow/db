monolog:
    channels:
        - deprecation
        - app_audit

when@dev:
    monolog:
        handlers:
            audit_db:
                type: service
                id: App\Service\Audit\DatabaseAuditHandler
                channels: ['app_audit']
            main:
                type: stream
                path: '%kernel.logs_dir%/%kernel.environment%.log'
                level: debug
                channels: ['!event']
            console:
                type: console
                process_psr_3_messages: false
                channels: ['!event', '!doctrine', '!console']

when@test:
    monolog:
        handlers:
            audit_db:
                type: service
                id: App\Service\Audit\DatabaseAuditHandler
                channels: ['app_audit']
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ['!event']
            nested:
                type: stream
                path: '%kernel.logs_dir%/%kernel.environment%.log'
                level: debug

when@prod:
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                buffer_size: 50
                channels: ['!event', '!app_audit']
            nested:
                type: rotating_file
                path: '%kernel.logs_dir%/%kernel.environment%.log'
                level: debug
                max_files: 14
                formatter: monolog.formatter.line
            audit:
                type: rotating_file
                path: '%kernel.logs_dir%/audit.log'
                level: info
                max_files: 90
                channels: ['app_audit']
            # Те же записи — в таблицу audit_event: журнал в панели администратора и выгрузка в SIEM.
            audit_db:
                type: service
                id: App\Service\Audit\DatabaseAuditHandler
                channels: ['app_audit']
            console:
                type: console
                process_psr_3_messages: false
                channels: ['!event', '!doctrine']
            deprecation:
                type: stream
                channels: [deprecation]
                path: '%kernel.logs_dir%/deprecations.log'
