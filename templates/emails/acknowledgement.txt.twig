Здравствуйте, {{ user.displayName }}!
{% if reminder %}

НАПОМИНАНИЕ: вы ещё не подтвердили ознакомление с документами:
{% else %}

Вам назначено ознакомление с документом:
{% endif %}
{% for d in documents %}
- {{ d.title }}{% if d.code %} ({{ d.code }}){% endif %} — {{ d.section.fullName }}: {{ urls[d.id] }}
{% endfor %}
{% if due %}

Срок ознакомления: {{ due|date('d.m.Y') }}{% if due < date('today') %} (просрочено){% endif %}.
{% endif %}

Откройте документ, прочитайте его и нажмите кнопку «Ознакомлен» в карточке документа.
Портал зафиксирует дату, время и номер редакции — это заменяет подпись в бумажном листе ознакомления.

--
Письмо отправлено автоматически порталом «{{ app_name }}». Отвечать на него не нужно.
